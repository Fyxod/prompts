Here’s a polished final prompt you can paste into the main session:

```text
I want to add a full UI on top of the existing geometric-v1 project.

Important:
- Do not disturb or break the current CLI functionality.
- These commands must continue working exactly as before:
  python run_pipeline.py --config pipeline.json
  python run_brute_force.py --config brute.json
  python run_batch_brute_force.py --config batch_brute.json
  python -m geometric_v1.pipeline --config pipeline.json
  python -m geometric_v1.brute_force --config brute.json
  python -m geometric_v1.batch_brute_force --config batch_brute.json
- Do not duplicate the pipeline, perturbation, brute force, or batch brute force logic.
- Reuse the existing Python functions and add optional progress/event callbacks where needed.
- The UI/backend should sit on top of the existing system.
- Do not use a src/ layout.

Build a local UI/backend for:
1. Perturb only
2. Diffuse only
3. Run full pipeline
4. Run plain brute force
5. Run batch brute force

Use the existing JSON files as the source of truth:
- pipeline.json
- brute.json
- batch_brute.json

The UI should load all options from these JSON files. The user should be able to make real-time temporary changes in the UI before starting a run, but these changes are for that specific run only. Do not write temporary UI changes back to the permanent JSON files. Permanent changes must still be made manually in the JSON files.

For every UI-triggered run, save the effective temporary config used for that run, for example:
output/ui_runs/<run_id>/
  effective_pipeline.json
  effective_brute.json
  effective_batch_brute.json
  events.jsonl
  report.json
  images/reports generated by the existing pipeline

Use a clean backend architecture. Prefer:
- FastAPI backend
- WebSocket or Server-Sent Events for real-time updates
- SQLite for indexing run history
- Existing output folders for actual images, reports, and configs

Do not make the UI scrape terminal output. Add structured progress events inside the Python runners.

Add an optional event callback system to the existing code, something like:

emit_event({
  "type": "deepface_model_done",
  "run_number": 12,
  "model": "SFace",
  "percentage": 45.2,
  "status": "completed"
})

The CLI should ignore these events when no callback is provided. The UI backend should stream them to the frontend in real time.

The event system should support events for:
- run started
- run completed
- run failed
- perturbation started/completed
- diffusion started/completed
- image written
- brute attempt started/completed/failed/skipped/resumed
- DeepFace model pending/running/completed/error
- running mean updated
- min/max score updated
- batch combo queued/running/completed/skipped/resumed/failed
- logs/status messages

UI requirements:

General:
- The UI should have separate sections/pages/tabs for:
  1. Perturb
  2. Diffuse
  3. Pipeline
  4. Brute Force
  5. Batch Brute Force
  6. History
- Each section should show previous runs for that section.
- The user should be able to inspect any previous run, including:
  - original image
  - perturbed image
  - original diffused image
  - perturbed diffused image
  - effective config
  - report.json
  - DeepFace results
  - sampled_config.json where applicable
  - event log if useful

For plain brute force:
- Show the current run number in real time.
- Show whether the attempt is new, skipped, resumed, successful, unsuccessful, or failure.
- Show the sampled perturbation options for the current run.
- Show the four image boxes in this layout:

  original image ------------------- perturbed image
       |                                  |
       |                                  |
  original diffused ---------------- perturbed diffused

- Empty boxes should appear before images are generated.
- When an image becomes available, display it immediately.
- During diffusion, show a spinner/progress state in the diffused image boxes.
- It is okay if diffusion only shows final images when complete. Do not add expensive intermediate diffusion image decoding unless it is simple and stable.

DeepFace display for brute force:
- Show all enabled DeepFace models.
- Each model should have states:
  pending
  running
  completed
  error
- Show a spinner while each model is running.
- As each model finishes, show its match percentage.
- If a model errors, show the error state and do not include it in the mean.
- Show the real-time mean of all completed successful model comparisons.
- Update the mean every time a model result comes back.

Brute force side/status panel:
- Show number of successful attempts.
- Show number of unsuccessful attempts.
- Show number of failures.
- Show number skipped/resumed if resume is active.
- Show lowest mean percentage seen so far.
- Show highest mean percentage seen so far.
- For both min and max, show:
  - run number
  - mean percentage
  - status
  - path/report link
  - button or link to inspect images and detailed report

The user should be able to inspect any brute-force run anytime:
- while the brute force job is still running
- after it finishes
- after restarting the UI

For batch brute force:
- Include all plain brute-force real-time details for the currently running attempt.
- Show the current image/prompt combination at the top.
- Show:
  - current image path
  - current prompt
  - combo index
  - total combos
  - current attempt inside combo
  - total attempts
- Show queue state:
  - queued combinations
  - running combinations
  - completed combinations
  - skipped combinations
  - resumed combinations
  - failed combinations
- Show per-combo min and max mean percentages.
- Show global min and max mean percentages across all combinations so far.
- The user should be able to inspect completed and currently running combinations.
- The user should be able to inspect any individual run inside any combination.

History/storage:
- Add a lightweight SQLite database to index UI runs.
- Store at least:
  - run id
  - run type: perturb, diffuse, pipeline, brute, batch_brute
  - status
  - started_at
  - finished_at
  - config paths
  - output path
  - report path
  - min score if applicable
  - max score if applicable
  - current progress summary
- Keep actual images/reports/configs in the output folders.
- Also save streamed events to events.jsonl in the run output folder.

Resume behavior:
- Integrate with the existing brute-force resume logic.
- If a brute or batch brute run is interrupted, rerunning from the UI should resume safely.
- Completed runs should not be overwritten.
- Incomplete runs should be resumed/archived according to the existing brute-force resume rules.
- Batch brute should skip only truly complete combos and resume incomplete combos.

Run control:
- Add Start buttons for each run type.
- Add a Stop/Cancel button if feasible.
- If full cancellation is hard, implement safe stop after the current attempt finishes.
- Make sure stopping does not corrupt completed run folders.

Frontend:
- Build a clean, functional local UI.
- Make the interface practical and information-dense.
- Avoid marketing/landing-page style.
- Use tabs/sidebar/navigation.
- Use clear status indicators, progress spinners, image previews, and report links.
- The UI should feel like an experiment control dashboard.

Backend/API:
- Add endpoints for:
  - reading base JSON configs
  - starting perturb run
  - starting diffusion run
  - starting pipeline run
  - starting brute run
  - starting batch brute run
  - streaming events for a run
  - listing history
  - reading a specific run report
  - serving generated images/files safely from output folders
  - stopping/canceling a run if implemented

Testing:
- Keep existing CLI tests working.
- Add smoke tests for backend functions where practical.
- Run a lightweight UI/backend smoke test.
- Run a tiny brute test with:
  - trials = 1 or 2
  - DeepFace reduced to SFace
  - diffusion steps = 1
  - max_size = 64
- Run a tiny batch brute test with:
  - 2 sample images
  - 2 prompts
  - trials = 1
  - SFace only
  - diffusion steps = 1
  - max_size = 64
- Verify real-time event emission works.
- Verify previous CLI commands still work.

Documentation:
- Update README with:
  - how to start the UI/backend
  - UI architecture
  - temporary UI overrides vs permanent JSON config
  - history storage
  - event streaming
  - brute-force real-time view
  - batch brute-force real-time view
  - resume behavior after interruption
  - any new dependencies
- Add any new requirements to requirements.txt.

Implementation guidance:
- Make small, clean commits as you build.
- Prefer adding reusable event hooks to existing functions instead of rewriting them.
- Do not break existing output folder structure.
- Do not permanently modify pipeline.json, brute.json, or batch_brute.json during UI runs.
- Commit and push the final working changes.
```

Tiny extra suggestion: ask the main session to implement this in phases if it starts getting large. The best first milestone is backend event hooks plus a simple live brute-force page. Once that works, batch brute becomes much easier.
